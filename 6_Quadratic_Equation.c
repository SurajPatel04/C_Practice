#include<stdio.h>
#include<conio.h>
void main(){
    double b,a,c,Quada;
    printf("Enter a number for b:");
    scanf("%f",&b);
    printf("Enter a number for a:");
    scanf("%f",&a);
    printf("Enter a number for c:");
    scanf("%f",&c);
    // The term b*b -4ac is known as the discriminant
    discriminant=(b*b)-(4*a*c);
    if (discriminant==)
    {
        
    }
    
}